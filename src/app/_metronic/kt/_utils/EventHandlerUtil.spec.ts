import { EventHandlerUtil } from "./EventHandlerUtil";

describe("EventHandlerUtil", () => {
  it("should create an instance of EventHandlerUtil", () => {
    expect(EventHandlerUtil).toBeTruthy();
  });
});
