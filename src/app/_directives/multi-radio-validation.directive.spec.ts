import { MultiRadioValidationDirective } from "./multi-radio-validation.directive";

describe("MultiRadioValidationDirective", () => {
  it("should create an instance", () => {
    const directive = new MultiRadioValidationDirective();
    expect(directive).toBeTruthy();
  });
});
